# =========================================================================
# LLM Configuration
# =========================================================================
LLM_PROVIDER=gemini
GOOGLE_API_KEY=your_google_api_key_here
OPENAI_API_KEY=your_openai_api_key_here

# =========================================================================
# STT Configuration (Speech-to-Text)
# =========================================================================
# WHISPER_MODEL options: tiny, base, small
# - tiny: 3x más rápido, menos preciso (RECOMENDADO para reducir latencia)
# - base: Balance velocidad/precisión (default)
# - small: Más preciso, más lento
WHISPER_MODEL=base
STT_LANGUAGE=es

# =========================================================================
# TTS Configuration (Text-to-Speech)
# =========================================================================
TTS_PROVIDER=elevenlabs
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here
# Voice ID de ElevenLabs (default: Rachel)
TTS_VOICE_ID=21m00Tcm4TlvDq8ikWAM

# =========================================================================
# Audio Configuration
# =========================================================================
SAMPLE_RATE=16000
CHUNK_SIZE=1024
# SILENCE_TIMEOUT_MS: Tiempo de silencio para considerar fin de turno
# - Valores bajos (500-750): Más responsivo, puede cortar palabras
# - Valores altos (1500-2000): Más tolerante, puede sentirse lento
SILENCE_TIMEOUT_MS=750

# =========================================================================
# Database Configuration
# =========================================================================
CHROMA_DB_PATH=./data/chroma_db

# MySQL (opcional - usa Mock si está desactivado)
USE_DATABASE=False
DB_HOST=localhost
DB_PORT=3306
DB_USER=hotel_user
DB_PASSWORD=hotel_password
DB_NAME=hotel_kiosk

# =========================================================================
# Performance Notes
# =========================================================================
# Para REDUCIR LATENCIA:
# 1. WHISPER_MODEL=tiny (reduce STT de ~5s a ~1.5s)
# 2. SILENCE_TIMEOUT_MS=750 (más responsivo)
# 3. Asegúrate de tener ELEVENLABS_API_KEY configurada (mejor calidad que Pyttsx3)
#
# Para MEJORAR CALIDAD:
# 1. WHISPER_MODEL=base o small (más preciso)
# 2. ELEVENLABS_API_KEY configurada (voz natural vs robótica)
# 3. SILENCE_TIMEOUT_MS=1500 (evita cortes prematuros)
